const router = require('express').Router();
// Import User model and withAuth 
const { User } = require('../models');
const withAuth = require('../utils/auth');

//Add routes for User and other models


module.exports = router;